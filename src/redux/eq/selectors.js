export const selectFilters = (state) => state.equipment.filters;
export const selectEq = (state) => state.equipment.all;
// export const selectEquippedNum = (state) => state.equipment.items;
export const selectLoading = (state) => state.equipment.isLoading;
export const selectError = (state) => state.equipment.error;
